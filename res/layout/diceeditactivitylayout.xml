<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/layout_root"
                android:orientation="horizontal"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="20dp">
    <EditText
            android:id="@+id/nameDiceText"
            android:editable="true"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:hint="@string/namehint"
            android:text="@string/empty"/>
    <NumberPicker
            android:id="@+id/sidespicker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/nameDiceText"
            android:layout_centerHorizontal="true"/>
    <NumberPicker
            android:id="@+id/multipicker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/nameDiceText"
            android:layout_toLeftOf="@id/sidespicker"/>
    <NumberPicker
            android:id="@+id/modpicker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/nameDiceText"
            android:layout_toRightOf="@id/sidespicker"/>
</RelativeLayout>